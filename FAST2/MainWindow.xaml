<Window x:Class="MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"

        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal" 
        TextOptions.TextRenderingMode="Auto"        
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}" 
        Title="FAST" 
        Icon="FAST-main-icon.ico" 
        WindowStartupLocation="CenterScreen" 
        Height="432" 
        Width="710" 
        MinWidth="710" 
        MinHeight="432">

    <Grid x:Name="mainGrid">
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="menuColumn" Width="1*" MinWidth="150" MaxWidth="200"/>
            <ColumnDefinition Width="4*" MinWidth="540"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="108"/>
            <RowDefinition x:Name="serverProfilesRow"/>
            <RowDefinition Height="141"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <Label  Content="Main" Margin="10,10,0,0" FontWeight="Bold" FontStyle="Italic"/>
            <ListBox x:Name="mainMenuItems" HorizontalAlignment="Stretch" Margin="0,0,0,10">
                <ListBoxItem x:Name="steamUpdaterTabSelect" Content="Steam Updater"/>
                <ListBoxItem x:Name="serverModsTabSelect" Content="Server Mods"/>
            </ListBox>
        </StackPanel>

        <StackPanel Grid.Row="1">
            <Rectangle Fill="Black" Height="2"></Rectangle>
            <StackPanel Orientation="Horizontal">
                <Label x:Name="serverProfilesLabel" Content="Server Profiles" Margin="10,10,0,0" FontWeight="Bold" FontStyle="Italic"/>
                <Button x:Name="newServerProfileButton" Style="{StaticResource MaterialDesignFloatingActionMiniAccentButton}" ToolTip="MaterialDesignFloatingActionMiniAccentButton" Height="15" Width="15" Margin="20,0,0,-10">
                    <materialDesign:PackIcon Kind="Plus" Height="10" Width="10" />
                </Button>
            </StackPanel>

            <ListBox x:Name="serverProfilesList" HorizontalAlignment="Stretch" Margin="0,0,0,10">
                <ListBoxItem Content="Default"/>
            </ListBox>
        </StackPanel>

        <StackPanel Grid.Row="2">
            <Rectangle Fill="Black" Height="2"></Rectangle>
            <Label Content="Other" Margin="10,10,0,0" FontWeight="Bold" FontStyle="Italic" />
            <ListBox x:Name="otherMenuItems" HorizontalAlignment="Stretch" Margin="0,0,0,10">
                <ListBoxItem x:Name="settingsTabSelect" Content="Settings"/>
                <ListBoxItem x:Name="toolsTabSelect" Content="Tools"/>
                <ListBoxItem x:Name="aboutTabSelect" Content="About"/>
            </ListBox>
        </StackPanel>

        <TabControl Grid.Column="1" Grid.RowSpan="3" x:Name="mainContent" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0,-2,0,0" Background="{x:Null}" BorderBrush="{x:Null}">
            <TabControl.ItemContainerStyle>
                <Style TargetType="{x:Type TabItem}">
                    <Setter Property="Visibility" Value="Collapsed"/>
                </Style>
            </TabControl.ItemContainerStyle>

            <TabItem x:Name="steamUpdaterTab" Header="TabItem">
                
            </TabItem>

            <TabItem x:Name="serverModsTab" Header="TabItem">
                
            </TabItem>

            <TabItem x:Name="settingsTab" Header="TabItem">
                
            </TabItem>

            <TabItem x:Name="toolsTab" Header="TabItem">
                
            </TabItem>

            <TabItem x:Name="aboutTab" Header="TabItem">
                
            </TabItem>
        </TabControl>

    </Grid>
</Window>
